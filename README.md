Create a virtual environment with uv
Activate the environment
run command - uv sync (to install dependencies)
add environment variables LLM_API_KEY

# to run the application 
run command - 
streamlit run app.py